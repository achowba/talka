<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>People</title>
</head>
<body>
    <p>Hello <%= user.username %>.</p>
    <ul>
        <% users.forEach(function(user) { %>
            <li>
                <a href="/chat/<%= user._id %>"><%= user.username %></a>
            </li>
        <% }); %>
    </ul>
</body>
</html>